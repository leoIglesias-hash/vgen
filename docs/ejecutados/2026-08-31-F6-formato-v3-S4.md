# F6 — revisión única de formato (S-4) — CERRADA 2026-08-31

Fase completa en dos días (2026-08-30/31), orden F6-1 → F6-3 → F6-2 → F6-4.
Resultado: **formato v3 adoptado** y el producto pasó a **1280@15 v3**
(`dcd6afb6…1632a`, 24.458.884 B = 62,8 % del mp4 fuente, 35,02 dB), instalado en
`outputs/` y servido como raíz de iargen.com/player/.

## Qué quedó construido

- **F6-1 — SPARSE diferencial** (`95c1f9c`): delta = `offset − prior − 1`;
  «creciente» pasa a ser estructural. Opt-in gateado por la VERSIÓN del header
  (nunca negociado desde el stream: la confusión de modo puede decodificar en
  silencio a otra matriz — fixture dirigido lo demuestra y se rechaza en ambas
  direcciones). Default off = stream v2 byte a byte.
- **F6-3 — envelope ASCLVID3** (`f3cc1ef`+`53e725b`+`1e16d18`): header de 20 B
  `<8sIII>` con `meta_len`; el sidecar ASCLSLOT viaja EMBEBIDO byte-exacto;
  versión envelope==interior siempre. Espejo JS completo (reader-v2, factory,
  3 players, verify) + `test_v3_cross.js` (round-trip Python↔JS byte-exacto).
  Spec §14. La meta embebida lleva solo GEOMETRÍA; valores/imágenes del overlay
  siguen llegando en runtime (datachannel, logo.png) — reemplazables sin
  re-encode.
- **F6-2 — barrido definitivo de tile** (`294c324` fix bench_ref v3): el barrido
  resultó **2D** (trellis espacial E-23, lossy, encoder × tile regional,
  lossless, transcoder). Ganador global: **espacial 16 + regional 32**, estable
  entre 768 y 1280. La config mixta se pinnea con `tile=sweep` (elección
  determinista — regla 5 verificada: dos pares de runs byte-idénticos).
- **F6-4 — CACHE-001** (`6fd23b6`): invalidación POR NOMBRE — puntero
  `clip.current.txt` (único mutable, no-cache/304) → `clip.<sha12>.asclv`
  immutable 1 año. `tools/make_versioned_clip.py` en el workflow encode;
  players con etapa de puntero y fallback total; verificado frío/caliente en
  producción.

## Acto de cierre (Instancia 029)

UN solo encode: run 33352859235 (`format=v3`, `tile=sweep`, extra
`--palette-refit 5 --near-lossless 8 --cols 1280`), wall 59:54. El v3 le ganó
71.576 B al v2 equivalente con calidad idéntica. Publicado por la vía manual
(token rotado vía API, PUTs con `x-sha256`, token quemado). Reproducción v3
verificada en navegador en producción (badge `ASCL v3 1280x720 @15fps`).

Evidencia: REGISTRO Instancia 029 + Carril F6 en RUNBOOK-ESTADO. Sigue F8.
