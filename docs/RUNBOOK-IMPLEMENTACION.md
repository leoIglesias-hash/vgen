# Runbook de implementación — formato propio híbrido

Estado: **nace el 2026-09-01 con ASCILINE-hybrid; alcance ampliado el mismo día
tras el debate de dirección, y método corregido esa misma tarde** (se descartó la
sonda sintética: se arranca emitiendo). Este archivo contiene SOLO las reglas de
ejecución y las tareas del paradigma nuevo.

El runbook del paradigma anterior (100 % JS: F10, F11, F8, DIAG-001 y opcionales,
todas **suspendidas**) está archivado verbatim en
[`historico/RUNBOOK-IMPLEMENTACION-asclv-js.md`](historico/RUNBOOK-IMPLEMENTACION-asclv-js.md);
si alguna se retoma, vuelve de ahí con decisión del operador, no se reescribe.

**Antes de ejecutar cualquier tarea de acá, leer
[`VISION-Y-OBJETIVOS.md`](VISION-Y-OBJETIVOS.md)** — es el norte del proyecto y
lo que evita que una sesión post-compact se desvíe. **El rumbo detallado
—evidencia, caminos, gates numéricos, orden y decisiones pendientes— está en
[`PLAN-IMPLEMENTACION-VGEN.md`](PLAN-IMPLEMENTACION-VGEN.md)** (vigente desde el
primer reporte de aparato, 2026-09-01); este runbook solo lleva las tareas.

## 0. Reglas de ejecución (heredadas, siguen todas vigentes)

1. **Commits directos a `main`, un commit por tarea.** Mensaje: `<ID>: <título>`.
2. **Regresión antes de cerrar.** La máquina de trabajo no tiene Python ni Node
   **a propósito**: la regresión completa corre en GitHub Actions (workflow
   `regression`) en cada push. Una tarea se cierra solo con ese CI en verde.
3. **Fila de registro obligatoria** para toda tarea que cambie bytes o fluidez
   medible. La medición en aparatos la hace el operador; su veredicto se
   transcribe textual.
4. **Una tarea no empieza si su precondición no está cerrada.**
5. **Ninguna tarea se cierra «porque compila».** El criterio de cierre está
   escrito y es verificable.
6. **Todo test nuevo se cablea en `tests/run_all.py` en el mismo commit.**
7. **Procedencia por sesión** en `RUNBOOK-ESTADO.md`.
8. **Se supone explícito, se reproduce, y recién ahí se normaliza** (regla
   corregida el 2026-09-01). Arrancar por suposición está permitido y es el
   método; lo que está prohibido es **normalizar** una suposición en la spec sin
   haberla reproducido. Toda suposición vive escrita con su refutación en
   [`EMISION-V0.md`](EMISION-V0.md) §4.
9. **Ningún aparato solo define el formato.** Un aparato puede refutar; para
   consagrar hacen falta al menos dos clases de aparato, o la decisión manual del
   operador (que siempre prevalece). Ver [`PLAN-DE-MEDICION.md`](PLAN-DE-MEDICION.md) §1.

## 1. El paradigma (decisión del operador, 2026-09-01)

Resumen ejecutable; el desarrollo está en
[`VISION-Y-OBJETIVOS.md`](VISION-Y-OBJETIVOS.md).

- **Construimos un formato propio, códec-agnóstico**, que se decide caro y
  offline y se reproduce **siempre por hardware**, y que se puede **intervenir en
  vivo sin re-codificar**. No es un player: es un paquete + un contrato de
  reproducción.
- **`<video>` es la única puerta al hardware**: todo lo que emitamos termina en
  algo que `<video>` acepta nativamente. Nada se decodifica en CPU propia.
- **Composición de linajes:** de **VP9/AV1** la compresión y las primitivas
  (golden frames, tiles, alfa en WebM); de **DASH** el modelo de datos y la
  intervención estructural (Periods, AdaptationSets, Representations); de **HLS**
  el piso de compatibilidad probado; de **ASCILINE** el máster determinista, la
  intervención matricial y la disciplina de medición.
- **Encoder caro, decoder sin estrés.** La filosofía madre no cambió; cambió
  quién ejecuta: antes un intérprete JS, ahora un bloque de silicio.
- **Base 1280×720 con fps variable** (operador): fijar la resolución es lo que
  vuelve intercambiables a las piezas y evita que el decodificador se reconfigure.
- **Dos capas:** `<video>` base + **un** canvas de intervención. Reemplaza al
  invariante de un-solo-layer del paradigma anterior.
- **El player JS no muere:** queda como reproductor de escritorio y banco de
  verificación del máster. Se mantiene, no crece.
- Evidencia que sostiene todo: REGISTRO, DIAG-002/003 (2026-09-01) — en la caja
  real el JS da 290 ms/cuadro contra 66,7 de presupuesto y el `producto.mp4`
  (4,1 MB) *«reproduce muy bien»*.

## 2. Fase H — formato híbrido

**Orden (actualizado al cierre de H-13, 2026-09-01 noche): ~~H-11~~ → ~~H-12~~ → ~~H-6~~ →
H-7 → H-8.** **H-6 ejecutada hasta la pantalla el 2026-09-05** (matriz + v1 publicada; falta la foto con `76`/`95`: [`EMISION-V1.md`](EMISION-V1.md)). **Orden vivo (2026-09-06, tarde): P-008 (encoder portátil, EN EJECUCIÓN por decisión del operador) → más ideas con el operador → H-24 (incentivador armado en pausa) → radio con la primera tecla → firma de H-7 → H-8 (muxer + archivo único).** H-8a se sostuvo en la caja; H-23 aparcada (sin tercer video: la ruleta es interacción). H-11 ejecutada hasta la pantalla (falta la foto de la caja). H-13 cerrada (REGISTRO «H-13: reporte de la caja»). H-9 cerrada; H-10 tiene la caja medida y queda
abierta para las otras clases. Se arrancó **emitiendo** (pack v0,
[`EMISION-V0.md`](EMISION-V0.md)) y se corrige reproduciendo. Regla de
dependencia: **H-7 no empieza sin H-13 y H-11 cerradas; H-8 no empieza sin H-7
aprobada por el operador.** H-14 y W-26 son independientes. La regla de avance:
**la fluidez es un gate, los bytes y el arranque son el objetivo**
([`PLAN-IMPLEMENTACION-VGEN.md`](PLAN-IMPLEMENTACION-VGEN.md) §3).

| ID | Archivo | Acción | Cierre |
|---|---|---|---|
| **H-0** | repo, `docs/` | **CERRADA 2026-09-01** (`8dae1e5`). Crear `ASCILINE-hybrid` (clon con historia completa, main + assets), mover a `docs/historico/` los diseños del paradigma JS, reescribir CLAUDE.md/runbooks/índices | repo en `leoIglesias-hash/ASCILINE-hybrid` con CI verde ✔ |
| **H-1..H-3** | — | **REEMPLAZADAS** el 2026-09-01 por el debate de dirección. Eran: diseño del player híbrido, investigación de emisión H.264, player mínimo. Absorbidas y ampliadas al pasar el alcance a «formato propio códec-agnóstico». **IDs no reusables** | — |
| **H-4, H-5** | — | **REEMPLAZADAS** el 2026-09-01 (misma tarde) por decisión del operador: eran la **sonda sintética de capacidades** y el **banco** como paso previo. Se descartaron porque hubieran fijado el formato contra **una sola TV box**. Su contenido útil está **disuelto dentro de H-9/H-10**: la página que reproduce el pack v0 reporta lo mismo, pero sobre material real y en varios aparatos. **IDs no reusables** | — |
| **H-9** | `tools/emit_pieces.py` + workflow `emitir-v0` | **CERRADA 2026-09-01** (run `33566441576`). El pack v0 de [`EMISION-V0.md`](EMISION-V0.md) §3 emitido desde el máster: Baseline, Main, VP9, VP9+alfa, más `hls-ts/`, `hls-fmp4/` y `dash/` **por remux**, y `MANIFEST.tsv`. Resumen y evidencia: [`ejecutados/2026-09-01-H9-pack-v0-y-herramientas.md`](ejecutados/2026-09-01-H9-pack-v0-y-herramientas.md) | piezas regenerables byte a byte con su sha256; CI verde ✔ (deja abierta **H-14**) |
| **H-10** | `frontend/v0.html` + `frontend/keypad.js` | **reproducirlo y que él nos diga.** **La TV box está medida (2026-09-01)**: reporte transcripto en el REGISTRO, fila en [`PLAN-DE-MEDICION.md`](PLAN-DE-MEDICION.md) §5, veredictos en [`EMISION-V0.md`](EMISION-V0.md) §4.b. Faltan el ojo del operador para alfa y VP9/HLS-TS (contador ciego), y las **otras clases** (celular, Smart TV, escritorio) — o su decisión manual de fijar la caja como clase que consagra | **abierta, no bloquea**: cierra con la tabla de §5 llena para ≥2 clases **o** con la decisión escrita del operador; veredictos textuales |
| **H-13** | `frontend/vgenfeed.js` (nuevo) + `frontend/v0.html` (crece) + `tests/test_vgenfeed.js` (nuevo) + `tests/test_v0_page.js` | **CERRADA 2026-09-01 noche** (código `85eebd1`→`b3d5837`, docs `0589dc9`; publicado en `v0/`; reporte de la caja transcripto en el REGISTRO; S9/S10/S12 marcadas en EMISION-V0 §4.c; camino del muxer escrito en el plan §2.3 y §4 H-8). Era: **por dónde entra el paquete** — con el pack ya publicado, **cero emisión nueva**. **(a) `vgenfeed.js`**, módulo ES5 compartido que H-8 va a reusar: `getBytes(url, cb)` (XHR `arraybuffer`), `concat(parts, mime)` → `Blob`, `feedMse(video, mime, urls, hooks)` (un `SourceBuffer`, anexo secuencial encadenado por `updateend`, `endOfStream` al final) y `switchTo(video, src, hooks)` (cambio de pieza midiendo pedido → primer avance de `currentTime`). Sin `Promise`, sin `JSON`. **(b) Cinco pruebas nuevas en `v0.html`**, todas sobre `dash/init.m4s` + `chunk-*.m4s` y las piezas progresivas publicadas: **`96` MSE H.264** (S9); **`97` Blob concatenado** (`init + 16 segmentos` en un Blob → `blob:`; S10; reporta también `duration`); **`98` intercambio de orden** (segmentos 1..8, 13..16, 9..12 por MSE y por Blob; S12); **`8` cambio a demanda** (VP9 → Baseline → VP9 por `src`, tres cambios, columna `cambio_ms` = pedido → primer avance; gate ≤ 1 s); **`99` bucle 60 s** (VP9 con `loop`, contando `seeking`/`waiting` por vuelta). **`5` corre las cinco en secuencia**; **`1` corre todo** (v0 + H-13). La tecla `8` deja de ser «solo hls-ts» (ese caso sigue dentro de `4`). Detección de `SourceBuffer.changeType` en la cabecera. **(c) Medición**: columna **`congel`** (muestras de 100 ms sin avance de `currentTime`, estando en `play`), «atascos» **cuenta solo después de arrancar**, la fila dice **«ciego»** cuando `total` no se movió, y **no se pausa al terminar** una medición (la pieza siguiente reemplaza `src`; la última queda sonando). Ningún dígito suelto queda demorado (regla del mando; `test_v0_page.js` lo verifica, más «hay 5 pruebas de paquete» y «existe la columna congel»). `test_vgenfeed.js` prueba el encadenado por `updateend` con un `SourceBuffer` falso y el orden de anexo | filas en el REGISTRO por camino con las columnas de [`PLAN-IMPLEMENTACION-VGEN.md`](PLAN-IMPLEMENTACION-VGEN.md) §3.1 (foto del reporte, tecla `95`); S9/S10/S12 marcadas en EMISION-V0 §4.c; gate ES5 y CI verdes; publicado en `v0/` (copia en `deploy/` antes); y **queda escrito qué camino implementa el muxer (H-8)** |
| **H-11** | `frontend/v0.html` (crece) + `tests/test_v0_page.js` | **CERRADA 2026-09-04 con la foto de la caja: ENCIMA** (0/155 caídos con canvas a 15 fps; REGISTRO «Visita a la caja 2026-09-04»). Era: **EJECUTADA hasta la pantalla 2026-09-01 noche** (`fd9a7ab`, CI verde, publicada en `v0/`; REGISTRO «H-11 implementada hasta la pantalla»). Falta la foto de la caja: `80` + `95`, `83` a ojo, ¿volvió el play? Era: **la bifurcación de layout**: canvas de intervención encima del `<video>` (número, ruleta, texto) con tres cargas —nada / rectángulo chico repintado a 15 fps / pantalla completa una vez—, sobre Baseline y VP9, midiendo caídos con y sin él contra la línea de base ya medida (0–2/155). Reusa `overlay.js`/`textlayer.js`/`slots.js`/`datachannel.js`; canvas dimensionado **al panel**, nunca a la superficie | S5 resuelta con números (caja + segunda clase, o decisión del operador): la intervención va **encima** o **al lado**, y queda escrito en DISENO §9; gate ES5 verde |
| **H-6** ✅ ejecutada 2026-09-05 (falta la foto) | `tools/emit_matrix.py`, `tools/emit_v1.py`, workflows `matriz-h6` y `emitir-v1`, teclas `72`/`74`/`75`/`76` | **matriz por bytes a igual look, con la fluidez como gate** (reorientada por el reporte: en la caja la fluidez está saturada y el decodificador es hardware). Ejes: VP9 (`crf`, `cpu-used`); **fps variable por segmento** derivado del máster (S6, ahora medido en bytes); H.264 piso relajado (Main/High, `refs`, B dentro del GOP cerrado 15); zonas estáticas; paleta consciente del 4:2:0. Emisión **v1**: cada variante con sus segmentos (CMAF para H.264; **WebM segmentado para VP9 → S11**), **con la pista de audio del máster muxeada** (AAC en mp4, Opus en WebM → **S13**) y una pista ambiente suelta para `<audio>` (→ **S14**). Incluye una fila de **referencia** con los defaults de ffmpeg (el `producto.mp4`) bajo los mismos contadores | tabla de variantes en el REGISTRO con medición **por aparato** y los gates de §3.1; **receta por perfil de dispositivo**, no una global, firmada por el operador |
| **H-12** | `frontend/vgencache.js` (nuevo) + `frontend/v0.html` (crece) + `tests/test_vgencache.js` (nuevo) + `tests/test_v0_page.js` | **CERRADA en lo que decide, 2026-09-04** (persiste al cierre de la app; cuota 225 MB; 25 MB entran; la tanda de 50 MB cerró la app → H-12b). Era: **EJECUTADA hasta la pantalla 2026-09-02** (`0ce2cb4` → `9011fe7`, CI verde, publicada en `v0/`; REGISTRO «H-12 implementada hasta la pantalla»). Falta la foto de la caja: `84` + `95`, **reiniciar**, `95` + `85`. Era: **caché**: descarga por XHR con progreso → `Blob` → **IndexedDB** → reproducción desde `blob:`, con pineo por contenido (CACHE-001) y borrado de claves viejas | el pack sobrevive a un reinicio del aparato y reproduce sin red; degradación escrita si no persiste |
| **H-7** ✏️ borrador 0.1 (2026-09-05 noche) | `docs/SPEC-VGEN.md` | **spec normativa**: layout binario, manifiesto (texto tabulado, **no JSON**: el gate ES5 lo prohíbe), segmentos, sprites, cues, huecos, y el mapa perfil → camino de runtime. Cierra las filas «gateadas» de [`DISENO-FORMATO-VGEN.md`](DISENO-FORMATO-VGEN.md) §10. **Estado:** escrita como contrato de lo que `producto.html` ejecuta, ⏳ en lo no reproducido, lista de firma en §12 | spec aprobada por el operador; **toda decisión trazable a una fila reproducida en ≥2 clases de aparato** |
| **H-8a** ✅ ejecutada 2026-09-05 (noche), falta la foto | `frontend/producto.html` + `frontend/GUION.tsv` (nuevos), `frontend/vgenfeed.js` (`ring`), `frontend/vgencache.js` (`budget/plan/join/part/ensure`), `tests/test_producto_page.js` (nuevo) | **el player mínimo del producto como prototipo** sobre el pack publicado: loop por anillo MSE `sequence` desde la caché, publicidad que reemplaza y vuelve (audio propio, radio en rampa), incentivador alfa encima, capa por papel, teclas de una cifra, reporte a dos columnas, vigilancia de `play()` | foto de la caja y del Smart TV con `1` ≥ 10 min, `5`, `4`, segunda apertura y red cortada (SPEC §12) |
| **P-008** 🔵 en ejecución 2026-09-06 | `tools/portable/armar.py`, `emitir.ps1`, `emitir.cmd`, `py.cmd`, `LEEME.md`; `.github/workflows/portable.yml`; `tests/test_portable_bundle.py` | **el encoder fuera del CI**: bundle portátil (Python embebido + ffmpeg estático + el mismo código), sin instalar nada; el workflow lo arma, lo publica y **compara sus bytes contra Linux** | CI verde + una corrida de `portable` con el gate leído: IDENTICA → el bundle publica; DISTINTA → manda el CI y se pinnea libvpx/x264 (ENCODER-PORTATIL §6-7) |
| **H-23** ⏸ aparcada 2026-09-06 (la caja la ve trabada con timer y con vsync; **no se reemplaza por video**: el tercer elemento es la ruleta, interactivo) | `frontend/producto.html` (`pedirLogo`, `paintLogo`, `contarPintada`, `7` cicla), `frontend/logo.png` (nuevo), `tests/test_producto_page.js` | **la imagen que gira encima del alfa**: `drawImage` rotado con el reloj del video sobre el incentivador, pedida una vez, con emblema de respaldo, costo por pintada en el reporte | foto de la caja y del Smart TV: **dentro del producto**, `7` dos veces (zócalo `capa num+img`), `4`, `9` (SPEC §12.6). La foto del 2026-09-05 la trajo apagada |
| **H-8** | `frontend/` + `backend/` | **muxer ES5 + player híbrido mínimo**: emisor del paquete offline y, en el aparato, **lo que H-13 haya dejado en pie**: concatenador CMAF (camino A, si S10 se sostiene), alimentador MSE (camino B, si S9), generador de playlist HLS-TS (camino D, reserva). `<video>` + `<audio>` separado + canvas de intervención. Incluye «cambiar solo la música» | reproduce en la caja real con intervención activa y dentro de los gates de §3.1 (veredicto del operador); gate ES5 verde; depende de **H-7** |
| **H-14** ✅ | `tools/emit_pieces.py` + workflow | **CERRADA: ejecutada en H-14b el 2026-09-04.** Era: **DECIDIDA 2026-09-04: adoptar `cpu-independent=1`** → se ejecuta como H-14b. Era: **EN CURSO 2026-09-02** (`0eb8dab`: `determinismo: true` emite las dos piezas H.264 dos veces en la misma corrida y las compara byte a byte; `--x264-extra` para probar `cpu-independent=1`; **causa establecida el 2026-09-02**: determinista en la misma máquina (9/9 pares idénticos), **distinto por CPU** (AMD 7763 = AMD 9V74 ≠ Intel; el pack publicado es el Intel) y **`cpu-independent=1` lo cura** (AMD 9V74 = Intel 8370C = Intel 8573C, +0,016 % de bytes). Falta la decisión del operador: adoptar la opción en la receta (recomendado) o redefinir el invariante 7). Era: **determinismo del carril H.264** (deuda abierta contra el invariante 7, detectada en H-9): dos corridas del mismo máster con los mismos parámetros dieron **bytes distintos en Baseline y Main**, con la misma versión de ffmpeg y la misma línea de opciones de x264; VP9 salió byte-idéntico. Emitir la misma pieza **dos veces dentro de la misma corrida** para separar «no determinista» de «depende de la máquina»; si es lo segundo, decidir entre fijar la codificación (p. ej. sin `mbtree`) o redefinir el invariante como «misma build + misma máquina». Registrar `lscpu` en el workflow | la causa queda establecida con evidencia y el invariante 7 vuelve a cumplirse, o se redefine por escrito con el visto del operador |
| **W-26** | `frontend/live-player.html` | **DECIDIDA 2026-09-04: terminar y republicar** → W-26b. **CERRADA EN CÓDIGO 2026-09-02** (`522bdf8` → `730c5f4`, CI verde; la raíz servida no se republicó, ver REGISTRO). Era: (heredada, independiente) la raíz publicada elige WebGL sin salida: aceptar `?renderer=` como las demás páginas y decidir el default para WebViews de TV box | la raíz respeta `?renderer=canvas2d`; gate ES5 verde |
| **H-14b** ✅ | `tools/emit_pieces.py` + workflow `emitir-v0` | **CERRADA 2026-09-04.** Hecho: `cpu-independent=1` en `X264_COMMON` (`bdc4a08`, CI verde, dos pruebas nuevas), pack re-emitido en `33894807627` (AMD 9V74) y `33894814769` (determinismo, AMD 7763) con bytes idénticos entre sí y con los Intel de H-14, 54 keys de `v0/` republicadas y verificadas (copia previa en `af1fc01`), REGISTRO «H-14b CERRADA», invariante 7 marcado saldado en EMISION. Era: **adoptar `cpu-independent=1`** (decisión del operador 2026-09-04, tras la explicación técnica: huellas estables para que la caché no se invalide por el runner). Acción: sumar `:cpu-independent=1` a `X264_BASELINE` y `X264_MAIN`; test que lo afirme; correr `emitir-v0` con `determinismo: true` **sin** `x264_extra` y verificar bytes idénticos entre pasadas; bajar el pack, publicar `v0/` con las huellas nuevas (copia previa en `deploy/`), fila en el REGISTRO con SHA-256 por pieza y la diferencia de bytes contra el pack del 09-01. Cierre: CI verde, dos pasadas idénticas en el log, huellas servidas = huellas del REGISTRO, invariante 7 marcado **cumplido** en EMISION §3.b |
| **H-12b** ⏳ | `frontend/vgencache.js` + `frontend/v0.html` + tests | **EJECUTADA HASTA LA PANTALLA 2026-09-04** (`432647b`, CI verde; `v0/index.html` y `v0/vgencache.js` publicadas y verificadas, copia previa en `b53a739`): los tres arreglos hechos y el límite de tanda cumplido en el módulo (`VGenCache.TANDA_MB`). El techo **quedo cerrado con la foto del 2026-09-04**: `entraron 50 MB (tope de la prueba)` con la app viva; y la cuota declarada no es un gate (13/225 MB al empezar, 43/225 despues con la base vacia). **Falta `85` tras apagar y prender, y `83` sola mostrando video.** Era: **cierre técnico de la caché** (foto del 2026-09-04: la tanda de 50 MB cerró la app; caídos −3; `83` sola en play). Acción: (1) techo en **tandas ≤ 5 MB** acumulativas hasta 50 MB, un registro por tanda, borrando al final, y la cuota declarada (`queryUsageAndQuota`) reportada como **primer techo**; nunca más de 5 MB de ruido vivos en RAM; (2) `blind()`/caídos: nunca negativos (reset del contador al cambiar de `src`); (3) `83` sola: arrancar el video con el mismo `play()` que usa el lote, y si el WebView exige gesto, decirlo en la fila («sin gesto»); (4) **REFORMULADO 2026-09-04 (noche, E16):** «arrancar sin red» se **descarta** —la app que hospeda al WebView tiene validaciones intermedias que piden red—, y cortar la red en el medio no prueba nada porque lo buffereado sigue sonando. La prueba que discrimina es **cortar la red con la página ya abierta y recién ahí `85`**, que lee IndexedDB y reproduce desde `blob:` sin tocar la red; para que la **foto** lo pruebe sola se agregó el campo **`red`** (`navigator.onLine`) a la cabecera y a la nota de las filas `cache:*`. Cierre: CI verde, PC sin filas negativas, techo por tandas en la PC, y la foto de la caja con `techo` completo sin cerrar la app |
| **H-16** ✅ | `frontend/v0.html` + `frontend/HoboStd.ttf` (versionada: lo desplegado se guarda antes de subirlo) + `docs/MANUAL-TECLAS-V0.md` (nuevo) + `tests/test_v0_page.js` | **EJECUTADA HASTA LA PANTALLA 2026-09-04** (`bbb92a1`+`52f8927`+`c4bb8ee`, CI verde; `v0/HoboStd.ttf` nueva y `v0/index.html` republicada, bucket 63 keys; manual escrito). Verificado en el navegador contra lo servido, no supuesto: fuente detectada en 165 ms sirviendose como octet-stream, 12 teclas -13 desde H-18- sin cortes a 1280 y a 420 de ancho, columna sin solape con el video, `83` inmediata arranca VP9 con la capa encima. La deteccion pasó de comparar una frase (margen de 1,5 px) a comparar M contra i (170 vs 53). **CERRADA 2026-09-04 con la foto de la caja**: `fuente hobo (166 ms)` en la cabecera y `fuente: hobo` en las tres filas de capa; la deteccion `MMMMM` contra `iiiii` no dio falso positivo. Era: **Hobo por defecto y reforma de la página** (operador 2026-09-04: «que sea la fuente por defecto así no agrego más funciones… quitá del `1` lo que ya tuvimos claridad… un archivo manual… al menos 10 siempre a la vista, alineada a la izquierda, dándole más lugar a los logs»). Acción: `@font-face` `Hobo` desde `v0/HoboStd.ttf`; la capa dibuja con `Hobo, monospace`; antes de la primera pintada, esperar hasta 3 s a que `measureText` cambie de ancho contra `monospace` (sin `document.fonts`, que es Promise) y anotar `fuente: hobo|fallback` en la nota de cada fila `capa*`; el `1` corre **solo lo no consagrado** (hoy: H-12b techo, H-18 cuando exista; lo consagrado —piezas progresivas, HLS/DASH, MSE, blob, capa, caché— se corre por su tecla); leyenda: ≥ 10 teclas vigentes en una columna a la izquierda, tabla de filas ocupando el resto del ancho y más alto; las teclas ocultas listadas en `docs/MANUAL-TECLAS-V0.md` (qué aprieta, qué mide, qué ya dijo la caja). Publicar `v0/` (html + ttf) con copia previa en `deploy/`. Cierre: CI verde, foto de la caja con la capa en Hobo y `fuente: hobo` en la fila |
| **W-26b** ⏳ | `deploy/asciline-player/` + las cuatro carpetas de la raíz | **EJECUTADA 2026-09-04.** Auditoría previa: 64 rutas de código en las cuatro carpetas, 56 iguales al repo y 8 distintas (la misma página en 4 carpetas; solo le faltaba W-26). 8 keys republicadas y verificadas (copia previa en `05d2b5a`, token quemado); `playloop.js` anotado en el manifiesto -estaba servido y sin registrar-; re-auditoría **64/64 iguales**. Medido en el navegador: sin el parámetro la raíz pasa **240 ms en WebGL** al abrir (1.476 → 1.714 ms), con `?renderer=canvas2d` nunca. **CERRADA 2026-09-04 (noche) con el reporte del operador:** *Â«sigue andando trabado pero sin pantallazosÂ»*. El escape hace lo suyo; lo trabado es el player 100 % JS, ya medido en DIAG-003 (cuello en CPU, no en WebGL), que es la razÃ³n por la que existe este repo. Era: **terminar W-26 como corresponde** (operador 2026-09-04: «dejemos eso cerrado»). Acción: bajar `index.html` y los `.js` servidos en `/`, `/1280-15/`, `/1280-12/`, `/1920-10/`; diff contra `frontend/live-player.html` + `playloop.js` + el resto; escribir en el REGISTRO qué cambia por carpeta (W-22..W-25 + W-26); republicar las keys que cambien con SHA verificado y token quemado; MANIFEST/README de deploy actualizados **antes** de subir. Cierre: las cuatro raíces sirven el `live-player.html` del repo, `?renderer=canvas2d` funciona en la caja (foto sin pantallazo blanco) |
| **H-18** ↻ | `frontend/v0.html` + tests | **EJECUTADA HASTA LA PANTALLA 2026-09-04** (`c1648b0`..`a564090`, CI verde; publicada). Tecla `87`: `v0-vp9` en bucle abajo y `v0-vp9-alpha` en un segundo `<video>` encima, del tamaño del rectángulo de la capa; la nota trae los cuadros de los dos. Dos gates viejos se **reformularon** (el `<video>` único pasa a «uno para las piezas + exactamente uno para el efecto»; la pausa única pasa a «el de las piezas sigue con una sola, la del `0`»). Corriendo la prueba dos veces seguidas apareció un contador que solo acertaba la primera: la base del de arriba ahora se arma con `currentTime > 0`. Medido: VP9 solo 0/156, con el segundo plano 5..12/157 abajo y 4..8/153 arriba. **REHECHA COMO H-18b** (el operador rechazo el armado: encogido, corrido y con el RGB del master). La caja alcanzo a decir 2/155 abajo y 2/138 arriba con la version mala. Era: **dos `<video>` a la vez** (pregunta del operador 2026-09-04: «¿probamos videos transparentes como para crear efectos que en realidad sean video?»). Acción: tecla que reproduce el loop (VP9) en el `<video>` principal y `v0-vp9-alpha` en un segundo `<video>` **encima**, posicionado como la capa (nunca creciendo hacia abajo, regla 10), con el contador en los dos; nota con caídos de cada uno. Cierre: fila en el REGISTRO; si sostiene → los efectos pueden ser piezas alfa (DISENO §9) y el techo de planos simultáneos pasa a ≥ 2; si no → los efectos van horneados o al canvas |
| **H-18b** ✅ | `tools/emit_pieces.py` + `tests/test_emit_pieces.py` + `frontend/v0.html` + `tests/test_v0_page.js` | **EJECUTADA HASTA LA PANTALLA 2026-09-04** (`8a6370a`+`d18c804`+`ddce1da`, CI verde). El operador rechazó el armado de H-18 por **dos razones distintas**: (a) el segundo `<video>` iba encogido al 26 % y corrido —se veía un recuadro flotando y además medía un video chico, no el del tamaño del de abajo—; (b) la pieza con alfa llevaba **el RGB del propio máster**, así que superpuesta exacta habría sido indistinguible de lo de abajo y la prueba no podía contestar si el navegador compuso. Acción: `placeEfecto` = exactamente el rectángulo del video; `confetti_rgba()` reemplaza a `alpha_channel`/`rgba_frame` y emite **papelitos sobre transparencia total** (160 rectángulos que caen, se hamacan y giran, ocho colores fijos, ~3 % de cobertura, RGB negro donde alfa es 0). Todo con **enteros y ondas triangulares, sin `sin`/`cos`**: 1 ULP entre dos libm movería un borde y cambiaría los bytes (invariante 7); un gate lee el código del generador y lo rechaza. **Obliga a re-emitir el pack y republicar `v0-vp9-alpha.webm` + `MANIFEST.tsv`.** **CERRADA 2026-09-04 (noche) con la foto**: `dos:vp9+alfa` = **2/154 abajo (1,3 %) y 1/141 arriba (0,7 %)**, los dos dentro del gate de 3 %, y el operador firmó *«con 2 va bien»*. **El efecto puede SER video** (DISENO §9). Era: Cierre: pack re-emitido y publicado con SHA verificado, y la foto de la caja con `87` mostrando un solo video con papelitos |
| **H-20** ✅ | `frontend/v0.html` + `tests/test_v0_page.js` + `docs/MANUAL-TECLAS-V0.md` | **EJECUTADA HASTA LA PANTALLA 2026-09-04** (`8a6370a`+`ddce1da`, CI verde). Dos pedidos del operador sobre la foto de esa noche. (1) **Pantalla entera** (*«no tenemos probado un fullscreen de estas cosas (que suele bajar rendimiento)»*): tecla `70` con cuatro escalones —`entera:solo`, `entera:capa`, `entera:dos`, `entera:todo`— porque lo que importa no es un número sino **dónde se rompe**; el video ocupa toda la superficie respetando 16:9 y centrado (estirarlo compararía contra otra cosa); se esconden encabezado, leyenda y tabla y queda el zócalo, que en una TV es la única forma de recordar cómo se sale; tecla `73` para verlo a ojo. La **API de fullscreen es un extra**: se pide y la fila declara `api si|no|sin api`, nunca se supone. (2) **Reporte en dos columnas** con `88` para volver: la foto cortó en la novena fila y en una TV no se scrollea ni se copia texto, así que lo que no entra no existe; la letra se busca **por medición** (1,40 em → 0,24 em) y decide el **alto medido**, no la estimación del reparto; deja de vivir en un `<textarea>`, donde el foco adentro apagaba el mando entero. Abre la **tercera puerta** del mando (`7`), porque el `9` estaba lleno y en el `8` quedaba un solo número. `80`/`81`/`82` bajan al manual (la foto los volvió a consagrar a 560×315) y `86` pasa a herramienta: siguen siendo **13 teclas a la vista**. **CERRADA 2026-09-04 (noche) con la foto**, que además entró **entera en una sola toma** —el `88` y las dos columnas hicieron lo suyo—. Medido: `entera:solo` 1/155 (**la superficie 4K no le cuesta al `<video>`: la escala el hardware**), `entera:capa` 0/153, `entera:dos` 4/156 + 2/138, y `entera:todo` **17/154 + 7/149**. La API **no se concedió** (`api no`) y no hizo falta. **Segunda corrida (3ª foto): el salto del cuarto escalón se repitió clavado (17/155 + 11/150) y los otros tres bailaron.** Pero el operador miró la pantalla: *«todo junto… se ven perfecto»* → **los tres planos quedan habilitados**; la regla «dos planos, no tres» que se había escrito por la mañana **se retiró esa misma noche** (la caja consagra; `quality no` desacredita al contador, E5). Era: Cierre: la foto de la caja con `70` y con el reporte entero en una sola toma |
| **H-21** ⏳ | `frontend/v0.html` + `tests/test_v0_page.js` + `docs/MANUAL-TECLAS-V0.md` | **EJECUTADA HASTA LA PANTALLA 2026-09-04 (noche).** Pedido del operador mirando la foto: *«se corta el video cuando son dos superpuestos. Eso esta mal.. podrias hacerme una opcion que sea un loop de los dos videos superpuestos? En pantalla grande»*. **Los cortes que vio son de la MEDICIÓN, no del aparato**: el `70` corre cuatro escalones y entre uno y otro para, cambia la carga y arranca de nuevo. Pero la pregunta de fondo no la contesta ningún contador —«2,6 % de caídos» y «se ve fluido dos minutos» son dos datos distintos, y el segundo lo firma el ojo (PLAN §3.5)—. Acción: tecla `71`, los dos planos a **toda la superficie, en bucle, sin un solo corte**, hasta que se apague; **no agrega fila** (no mide, muestra) pero el zócalo lleva los **caídos vivos de los dos**, para que el ojo y los números se miren juntos. El reloj se apaga en `stopAll()`, no en la tecla, para no llamarse en círculo. Verificado en el navegador contra lo publicado: los dos `<video>` en el mismo rectángulo de 1280×720, componiendo, y el zócalo contando. Cierre: CI verde, publicada, y la foto de la caja con `71` |
| **H-22** ✅ | `frontend/keypad.js` + `frontend/ir.html` + `frontend/v0.html` + `tests/test_keypad.js` + `tests/test_ir_page.js` + `tests/test_v0_page.js` | **EJECUTADA HASTA LA PANTALLA 2026-09-04 (noche).** Reporte del operador: *«al cargar ir.html no me toma los numeros ni del control remoto ni de un pad numerico que le conecte»*, en el WebView de un **Smart TV con Android**. Que fallen las dos entradas descarta el control y apunta a la página. Dos causas posibles y las dos atacadas: (a) `digitOf()` preguntaba **solo** por `keyCode`, y hay WebViews que mandan **`keyCode 0`** poblando únicamente `key` —**confirmado en el navegador de esta sesión**, que reproduce el síntoma: `keydown kc=0 w=0 cc=0 key=9`—; (b) el `<input>` de `ir.html` se lleva el foco y el manejador se retira ante un INPUT, el mismo defecto que el `<textarea>` de H-20. Acción: **cuatro caminos** para leer un dígito (`keyCode` 48–57, 96–105, `key`, `code`/`charCode`), **`keypress` como plan B** con guarda anti-doble, enganche en `document` y `window` con el evento marcado, el campo fuera del recorrido (`tabindex=-1`) con el foco devuelto al `body` al armar la página y `Volver` para soltarlo, y **una línea de diagnóstico** —fija en `ir.html`, en el zócalo en `v0/`— con `type/keyCode/which/charCode/key/code/foco`. Verificado contra lo servido: `3` navega y `83` enciende la capa **en el aparato que mandaba `keyCode 0`**. **CERRADA 2026-09-04 (noche) con la foto del Smart TV**: los números entran y el operador firmó *«anda aun mejor asi que pasa perfecto»*. La causa era la (a), como decía el navegador. La línea de diagnóstico **queda igual**: el próximo aparato puede fallar por la otra. Era: Cierre: CI verde, `v0/` publicada, `ir.html` entregado, y el reporte del Smart TV |
| **H-15** | `docs/SPEC-VGEN.md` (H-7) + `frontend/vgencache.js` (H-8) | **residencia** (requisito del operador 2026-09-04): el aparato está prendido ≥ 16 h/día; baja **una vez** (a lo sumo una por día, chequeo del manifiesto) y reproduce **siempre desde el aparato**; si se corta internet no falla; nada de «falso streaming». Manifiesto: `residente: si|no` y `prioridad` (entero, menor = antes) por pieza; presupuesto fijo por navegador = `min(tope_absoluto, fracción × cuota_declarada)` con valores manuales del operador por encima del automatismo; al pasarse, se conserva en orden de prioridad (incentivador → publicidad → resto) y lo que no entra se marca «por red». Cierre: diseño aprobado en H-7 y runtime medido en H-8 (arranque desde caché ≤ 1 s en la caja, y reproducción completa con la red cortada) |

**Pendiente externo (no es tarea nuestra):** pedirle a la app de la caja que el
WebView reporte el tamaño real del panel — hoy da 3840×2160 sobre un panel de
1280×720 (9× de píxeles regalados al compositor, medido en DIAG-003). Beneficia
también al carril `<video>`.

**Anotado sin tarea** (en [`DISENO-FORMATO-VGEN.md`](DISENO-FORMATO-VGEN.md)
§11): nivel N4 de intervención (intercambio sub-cuadro por tiles/slices), cuadros
sostenidos escritos a mano, golden frames de VP9, composición offline de escenas,
y la nota de que F10 —suspendida— seguiría mejorando el producto porque el video
hereda los píxeles del máster.

## 3. Definición de terminado

Una tarea está cerrada cuando, y solo cuando:

1. la regresión completa pasa (CI en verde sobre su push);
2. su criterio de cierre escrito se cumple y se verificó, no se supuso;
3. si cambió bytes o fluidez, su fila está en el REGISTRO;
4. el commit lleva su ID;
5. si tocó el frontend, el gate ES5 pasa;
6. lo que requiere pantalla lo firma **el operador**, nadie más;
7. si es una tarea de medición, **ninguna celda quedó estimada**: o está medida o
   está vacía — y **ninguna conclusión sale de un solo aparato**.

El avance se registra en [`RUNBOOK-ESTADO.md`](RUNBOOK-ESTADO.md): una fila por
tarea. Ese archivo —no la memoria de nadie— es lo que le dice a la próxima
sesión dónde quedó todo.
