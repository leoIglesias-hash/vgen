# Dependencias Python para CREAR (.asclv). Para SOLO reproducir no se necesita nada de esto.
Pillow>=9
numpy>=1.21
opencv-python-headless>=4.5   # 'headless' = sin GUI, ideal para servidor
imageio-ffmpeg>=0.6           # fallback portable si ffmpeg no esta instalado globalmente
# Tambien puede usarse el binario del sistema 'ffmpeg'; tiene prioridad si esta disponible:
#   Debian/Ubuntu : sudo apt install ffmpeg
#   macOS (brew)  : brew install ffmpeg
#   Windows       : https://ffmpeg.org/download.html  (agregar al PATH)
